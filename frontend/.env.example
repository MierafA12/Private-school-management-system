# Points to your running backend through the Vite dev proxy
# In dev keep this as /api — the vite.config.js proxy forwards it to localhost:5001
VITE_API_URL=/api

# In production, change this to your deployed backend URL:
# VITE_API_URL=https://your-backend.com/api
