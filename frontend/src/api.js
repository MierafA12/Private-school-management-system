/**
 * Legacy API bridge
 * Re-exports all domain API modules and client utilities from `./api/index.js`.
 * Provides 100% backward compatibility with existing imports across the application.
 */

export * from './api/index';
